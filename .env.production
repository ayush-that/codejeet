# Loaded during `next build` (including Cloudflare Workers Builds).
# Publishable keys are safe to commit — they ship in the client bundle anyway.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuY29kZWplZXQuY29tJA